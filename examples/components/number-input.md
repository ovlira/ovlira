# Number input

```html
<ov-number-input label="Seats" name="seats" min="1" max="24" step="1" value="4"></ov-number-input>
```

Use a number input for bounded quantities and counts. Keep the value as a string at the component boundary so an empty field can be represented while the owning form validates the numeric domain.
