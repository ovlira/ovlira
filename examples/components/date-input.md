# Date input

```html
<ov-date-input label="Launch date" name="launch-date" min="2026-01-01"></ov-date-input>
```

Use ISO `YYYY-MM-DD` values in application state. Keep date-range and time-zone rules in the owning workflow.
