# Toast

```html
<ov-toast tone="success" heading="Saved" open>
  Your project is up to date.
</ov-toast>
```

The owning application decides where to place the toast and should keep a live region available for transient messages. Set `duration="0"` when a message must remain until dismissed.
