# Slider

```html
<ov-slider label="Opacity" min="0" max="100" value="68" show-value></ov-slider>
```

Use meaningful range and step values, and keep the visible value in sync with the application state when the selected number affects the task.
