# Checkbox

```html
<ov-checkbox label="Keep me signed in" name="remember" checked></ov-checkbox>
```

Use a visible label for each independent option. Wrap related options in a native `<fieldset>` with a `<legend>`.
