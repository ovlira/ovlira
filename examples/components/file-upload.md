# File upload

```html
<ov-file-upload label="Project archive" accept=".zip" name="archive"></ov-file-upload>
```

Listen for `change` and validate the selected files in the owning application. The native picker remains available for keyboard and assistive technology users, while the same field also accepts dropped files.
