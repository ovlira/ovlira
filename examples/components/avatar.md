# Avatar

```html
<ov-avatar name="Maya Chen" status="online"></ov-avatar>
```

Use a meaningful name. An optional image is supported, with initials as the fallback when the image is missing or cannot load.
