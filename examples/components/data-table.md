# Data table

```ts
const table = document.querySelector('ov-data-table')!;
table.caption = 'Recent projects';
table.columns = [{ key: 'name', label: 'Name' }];
table.rows = [{ name: 'Northstar' }];
```
