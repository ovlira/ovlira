# Settings page example

This is the runnable example produced by:

```bash
ovlira init ./settings-page
ovlira add page.settings --cwd ./settings-page
cd settings-page
npm install
npm run dev
```

The page uses the application shell, page header, cards, inputs, selects, buttons, and alerts. Its loading, error, and success markers are present so the recipe can be checked statically.
