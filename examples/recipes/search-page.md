# Search/results page

Use a visible query field, keep the query after submission, and make loading, empty, and error states explicit. Recommended sequence: page header → search controls → result summary → table or empty state.

The generated starter includes a status filter, sample results, and a preview switcher for results, loading, empty, and error. Keep the query and filters as the stable seam for the real search implementation.
