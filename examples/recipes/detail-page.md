# Detail page

Lead with record identity and status, group related fields into aligned sections, and separate destructive actions from the primary edit action.

The generated starter includes identity and context sections plus ready, loading, and error preview states. Replace the example record with the domain model while keeping the identity-first hierarchy.
