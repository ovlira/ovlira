# CRUD table page

Make create the page-level primary action, keep row actions concise, and preserve context after a destructive action. Required states: loading, empty, error, and success.

The generated starter includes a populated table, an empty-state action, and a small preview switcher so an agent can wire the collection's real fetch and mutation states without changing the base components.
