# Application shell recipe

Use native links inside the navigation slot and keep the persistent frame useful at narrow widths.

The generated starter includes a persistent rail, utility header, page header, and two simple overview cards. Replace the navigation labels and content slots with the product's own screens and data.
