// Register the shipped custom elements for the catalogue preview. Astro bundles
// this client entrypoint so the static marketing build remains self-contained.
import '../../../src/components/index.ts';
